# Railway Deployment Environment Variables
# Copy this file to .env or configure in Railway Dashboard

# Application Configuration
NODE_ENV=production
PORT=3001

# Database Path (Railway Volume should be mounted at /app/data)
# This is automatically set by the code based on NODE_ENV

# File Upload Path (Railway Volume)
# This is automatically set by the code based on NODE_ENV

# Optional: If you want to add any custom configurations
# Add them here or in Railway Dashboard Variables tab
